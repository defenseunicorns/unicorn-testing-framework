name: Package and Test
run-name: ${{github.actor}} is running the package workflow
on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - 'examples/after-deployment-validation-zarf/**'
      - 'utils/k3d/**'
  push:
    branches: [main]
    paths:
      - 'examples/after-deployment-validation-zarf/**'
      - 'utils/k3d/**'
concurrency:
  group: ${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true
permissions:
  contents: read
defaults:
  run:
    shell: bash
env:
  BRANCH_NAME: "${{ github.head_ref || github.ref_name }}"
  SHA: "${{ github.sha }}"
  K3D_TAG: "v5.4.9"
jobs:
  ##########
  # Zarf package build
  ##########
  package-podinfo:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: Install Zarf
        uses: defenseunicorns/setup-zarf@main
        with:
          version: v0.26.1
          download-init-package: true

      - name: Create Zarf Package
        working-directory: examples/after-deployment-validation-zarf
        run: |
          # build the zarf package
          zarf package create \
            --confirm \
            --skip-sbom

      - name: Create k3d Cluster
        working-directory: utils/k3d
        run: |
          wget -q -O install.sh - https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | TAG=${ env.K3D_TAG } bash
          k3d version
          ./k3d_cluster.sh create

      - name: Zarf Init
        run: zarf init --confirm

      - name: Deploy Podinfo
        run: zarf package deploy zarf-package-after-deployment-validation-amd64.tar.gz --confirm

      - name: List installed packages
        run: zarf package list
